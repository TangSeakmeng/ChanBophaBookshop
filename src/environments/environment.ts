// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAfchTiWccFODA7J9iL3JV2MEwlc_rctO0",
    authDomain: "chanbophastationery-c903d.firebaseapp.com",
    databaseURL: "https://chanbophastationery-c903d.firebaseio.com",
    projectId: "chanbophastationery-c903d",
    storageBucket: "chanbophastationery-c903d.appspot.com",
    messagingSenderId: "441554626487",
    appId: "1:441554626487:web:e55800780aab063c242a32",
    measurementId: "G-LPBR0NHVJH"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
